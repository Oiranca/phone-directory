export const NotFoundPage = () => (
  <section aria-labelledby="not-found-heading" className="rounded-3xl bg-white p-6 shadow-panel">
    <h2 id="not-found-heading" className="text-2xl font-semibold text-scs-blueDark">Pantalla no encontrada</h2>
    <p className="mt-2 text-sm text-slate-600">
      No hemos encontrado esta página. Puede que el enlace esté desactualizado o que la dirección tenga un error; vuelve al directorio para seguir trabajando.
    </p>
  </section>
);
