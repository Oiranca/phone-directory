#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"

if [[ -n "${SKIP_PRECOMMIT_CI:-}" ]]; then
  echo "Skipping pre-commit CI because SKIP_PRECOMMIT_CI is set."
  exit 0
fi

bash "$REPO_ROOT/scripts/run-precommit-ci.sh"
